//Arrow function
function add(a, b) {
    return a + b;
}

const addArrow = (a, b) => a + b;

const user = (id, name, age, address) => {
    {
        id, name, age, address
    }
}
//Callback

//Promise

//Async/Await